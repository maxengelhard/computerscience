#include <stdio.h>

int main(void)
{
    int x = 10;
    int y = 20;

    if (x < y ) {
        printf("x is less than y\n");
    }
    else if (x > y ) {
        printf("x is less not than y\n");
    }
    else {
        printf("x is equal");
    }
}